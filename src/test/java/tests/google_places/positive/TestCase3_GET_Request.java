package tests.google_places.positive;import io.restassured.RestAssured;import io.restassured.http.Header;import io.restassured.http.Headers;import io.restassured.http.Method;import io.restassured.response.Response;import io.restassured.specification.RequestSpecification;import org.testng.Assert;import org.testng.annotations.Test;public class TestCase3_GET_Request {    @Test    void getGoogleApiHeaderValid(){        //Specify base RRI        RestAssured.baseURI="https://postman-echo.com";        //Request object        RequestSpecification httpRequest = RestAssured.given();        //Response object        Response response = httpRequest.request(Method.GET, "/headers");        //Print response//        String responseBody = response.getBody().asString();//        System.out.println("Response body is: " +responseBody);        //Print all headers        Headers allHeaders = response.headers();        // Iterate over all the Headers        for(Header header : allHeaders)        {            System.out.println("Key: " + header.getName() + " Value: " + header.getValue());        }        //Capture details of headers from response        String hostType = response.header("Server");        System.out.println("Show me host: " +hostType);        Assert.assertEquals(hostType, "nginx");    }}